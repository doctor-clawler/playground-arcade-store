Original prompt: 이 게임은 건축게임이야 아래에 있는 만들기 버튼을 누르면 여러가지 건축물들이 나와 블록:자작나무,진한색나무,연한색나무,유리,물블록,벽돌,콘크리트 가구:침대,의자,2층침대,액자,책장,책상,서랍,조명 욕실:욕죠,변기,휴지,샤워부스,수건,새면대,세탁기 주방:오븐,주방용서랍,전자레인지,주전자,식탁,의자,냉장고,스테이크,케이크,물,파스타,햄버거,고등어,밥 외부:그네,풀,시소,미끄럼틀,의자 학교:사물함,칠판,가방,책,교탁,책상,의자 창문:가로창문,세로창문,작은 창문,큰 창문 모든 블록은 3D이고 맨 위에는 저장하고 나가기를 눌러줘 누르면 저장이돼

## 2026-06-20

- 새 self-contained Vite/Three.js 웹 게임 폴더를 만들기로 결정.
- UI 구조: 상단 저장하고 나가기, 하단 만들기 버튼, 열리는 카테고리/아이템 패널, 중앙 3D 건축판.
- 구현 완료: Three.js 3D 건축판, 7개 카테고리, 요청된 52개 아이템, 선택/회전/배치, localStorage 저장과 복원.
- 검증 완료: `npm run build`, web-game Playwright 클라이언트 캔버스 캡처, 자작나무+침대 배치/저장/reload 복원 스모크, 전체 52개 아이템 생성 스모크.
- 추가 구현: 하단 `만들기` 옆 `삭제` 버튼. 누르면 삭제 모드가 켜지고, 원하는 3D 블록을 클릭하면 해당 블록만 삭제된 뒤 삭제 모드가 꺼진다.
- 추가 검증: `npm run build`, `npm audit --json`, web-game Playwright 캡처, 자작나무+침대 배치 후 자작나무 삭제/저장/reload 복원 스모크.
- 추가 수정: 블록을 기존 블록 위에 쌓을 수 있게 배치 높이 `y`를 저장/복원한다. 선택한 블록으로 기존 블록을 클릭하면 같은 격자 칸의 최고 높이 위에 올라간다.
- 추가 검증: 같은 자리에 자작나무 2개 배치 시 `y=0`, `y=1` 저장/reload 확인, 삭제 회귀 확인, `npm run build`, `npm audit --json`.
- 추가 수정: 물블록 위의 흰색 원형 하이라이트를 제거했다.
- 추가 검증: `npm run build`, web-game Playwright 캡처, 물블록 배치 스모크와 스크린샷 확인.
- 버그 수정: 블록 옆면을 클릭할 때 첫 블록 위로 잘못 쌓이던 문제를 수정했다. 위면 클릭은 같은 칸 위로 스택, 옆면 클릭은 면 방향의 옆 칸 배치로 분리했다.
- 추가 안정화: 마우스가 실제로 움직이지 않았는데 `pointermove`가 다시 들어와 미리보기 칸이 바뀌는 경우를 막아, 보이는 미리보기 위치와 실제 클릭 배치가 일치하게 했다.
- 추가 검증: 같은 자리 재클릭은 `y=0/1` 스택, 옆 이동 후 클릭은 다른 칸 `y=0` 배치, 삭제 회귀, web-game Playwright 캡처, `npm run build`, `npm audit --json`.
